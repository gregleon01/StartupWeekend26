# StartupWeekend26

Fresh repo for the Startup Weekend 2026 swarm workflow.

## Purpose
- Serve as the clean base repository for per-role Git worktrees.
- Hold the winning concept, prototype, validation artifacts, and pitch assets.

## Workflow
- `startup-weekend-swarm/coord/` contains shared coordination files.
- `startup-weekend-swarm/worktrees/` contains one worktree per role.
- Keep `main` clean and merge role branches intentionally.
